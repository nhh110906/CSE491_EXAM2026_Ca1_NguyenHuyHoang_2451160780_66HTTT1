# reactjs — Câu 2 (React + Tailwind)

Cấu trúc component giống `mau1/reactjs`, UI dùng Tailwind CDN.

```
src/components/
├── Header.jsx
├── ProductForm.jsx
├── ProductTable.jsx
├── ProductRow.jsx
└── ProductStatusBadge.jsx
```

```bash
npm install && npm run dev
npm run build && open dist/index.html
```
